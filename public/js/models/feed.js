define([],function(){
           var Feed = Backbone.Model.extend({
                                                initialize: function() {
                                                }
                                            });
           return Feed;
       });
